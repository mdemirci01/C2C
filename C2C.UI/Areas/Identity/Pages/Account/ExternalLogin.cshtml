@page
@model ExternalLoginModel
@{
    ViewData["Title"] = "Kayıt Ol";
}

<h2>@ViewData["Title"]</h2>
<h4> @Model.LoginProvider Hesabı etkinleştir.</h4>
<hr />

<p class="text-info">
    <strong>@Model.LoginProvider</strong> ile başarıyla doğruladınız.

    Lütfen bu site için bir e-posta adresi girin ve bitirmek için Kayıt düğmesine tıklayın.
    Giriş.
    
</p>

<div class="row">
    <div class="col-md-4">
        <form asp-page-handler="Confirmation" asp-route-returnUrl="@Model.ReturnUrl" method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Input.Email"></label>
                <input asp-for="Input.Email" class="form-control" />
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-default">Kayıt ol</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}

