@page
@model LoginWithRecoveryCodeModel
@{
    ViewData["Title"] = "Kurtarma kodu doğrulaması";
}

<h2>@ViewData["Title"]</h2>
<hr />
<p>
    Bir kurtarma koduyla giriş yapmanız istenmiş. Oturum açarken bir kimlik doğrulama uygulaması
    kodu sağlayıncaya veya 2FA'yı devre dışı bırakıp tekrar giriş yapana kadar bu oturum açma işlemi hatırlanmayacaktır.
</p>
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Input.RecoveryCode"></label>
                <input asp-for="Input.RecoveryCode" class="form-control" autocomplete="off" />
                <span asp-validation-for="Input.RecoveryCode" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-default">Giriş yap</button>
        </form>
    </div>
</div>
 
 @section Scripts {
    <partial name="_ValidationScriptsPartial" />
}